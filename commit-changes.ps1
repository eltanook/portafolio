# Commit and push changes
git commit -m "Add Strapi API routes controllers and services + Update texts and email"
git push origin main

Write-Host "`nChanges committed and pushed successfully!" -ForegroundColor Green
Write-Host "Render will automatically redeploy Strapi backend in ~5-10 minutes" -ForegroundColor Yellow
